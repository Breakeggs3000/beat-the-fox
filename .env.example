# Stripe keys - use a restricted key (rk_ prefix) in production
# Generate one at: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=your_stripe_key_here

# Your deployed app URL (no trailing slash)
# In development, vercel dev uses http://localhost:3000 automatically
APP_URL=https://your-project.vercel.app
